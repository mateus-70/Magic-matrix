/*
#include "/home/mateus/ban/math/programs/utils/definitions.h"

struct func {
    char name;
    char polynomial_exp[40];
    double coeficients[10];
};

int order_f(struct func *f){
    int i=j=k=0;


    
return 0;
}

int insert_polynomial(struct func function){
    
    // declaration goes on main?
    printf("Give the polynomial: ");
    fgets( function.polynomial_exp, 40, stdin);
    printf("The pol given is: %s", function.polynomial_exp);

return 0;
}
*/
