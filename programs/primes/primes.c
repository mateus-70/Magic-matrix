#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#define N_MAX_PRIMOS 1000000000000000000 // 18 zeros + 1 , 19 dígitos

int main()
{
    // Variaveis
    int primos[N_MAX_PRIMOS] = {};

    /* Corpo */

    // Verificar se é primo
    /*
    for(i=0; i < N_MAX_PRIMOS + 1 ; i++)
    {
        if( n % primos[i] != 0){
            // 'n' não é divisível pelo primo[i]
            ch_primo = 'f'
        }
    }
    */

    if (1 % 0 == 10) puts("FILHA DA PUTA");

    return 0;
}
